{{#each colors}}
$md-colors-{{@key}}: (
{{#each this}}
  {{@key}}: {{this}},
{{/each}}
);

{{/each}}
$md-colors: (
{{#each colors}}
  {{@key}}: $md-colors-{{@key}},
{{/each}}
);

@function md-color($color, $spectrum) {
  $map: map-get($md-colors, $color);
  $hex: map-get($map, $spectrum);
  @return $hex;
}
